package chapter09.query.unification;

import org.drools.devguide.eshop.model.Item;
import org.drools.devguide.chapter09.model.IsPartOf;


query isItemContainedIn(Item p, Item w) 
    IsPartOf(w := whole,  p := part)
end